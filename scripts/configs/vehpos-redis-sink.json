{
  "name": "vehpos-redis-sink",
  "config": {
    "connector.class": "com.redis.kafka.connect.RedisSinkConnector",
    "tasks.max": "1",
    "topics": "gtfs.vehicle.positions.sl",

    "redis.uri": "redis://redis:6379",
    "redis.type": "HASH",
    "redis.keyspace": "gtfs:vehicle",
    "redis.key.ttl": "120",

    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "io.confluent.connect.avro.AvroConverter",
    "value.converter.schema.registry.url": "http://kafka-schema-registry:8081",

    "transforms": "ValueToKey,ExtractFieldKey",
    "transforms.ValueToKey.type": "org.apache.kafka.connect.transforms.ValueToKey",
    "transforms.ValueToKey.fields": "trip_id",
    "transforms.ExtractFieldKey.type": "org.apache.kafka.connect.transforms.ExtractField$Key",
    "transforms.ExtractFieldKey.field": "trip_id"
  }
}
